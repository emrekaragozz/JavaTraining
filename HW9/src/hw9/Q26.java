package hw9;

import java.util.function.*;

public class Q26 {
	public static void main(String[] s) {
		
		//Lambda expression's parameter o is expected to be of type String
		//variable name s already used as parameter of main method
		
		
		/*
		Predicate<String> pred = (Int i) -> true;
		Predicate<String> pred = (Object o) -> true;
		Predicate<String> pred = (String s) -> true;
		*/
 }
}
