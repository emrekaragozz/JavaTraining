package hw4;

//Class name must be same with the file name, FirstName is changed to Q44
public class Q44 {
    public static void main(String[] names) { //no arguments are passed
        System.out.println(names[0]);
        //throws ArrayIndexOutOfBoundsException because it is an empty array
    }
}
