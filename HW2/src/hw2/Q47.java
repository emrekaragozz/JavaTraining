package hw2;

// Class name must be same with the file name, TennisBall is changed to Q47
public class Q47 {
    public Q47() { // constructor

    }
    //public Q47  static create() { return new Q47 (); } is a static method, not a constructor
    //public Q47  static newInstance() { return new Q47 ():} is a static method, not a constructor, ";" is needed
    //public void TennisBall() {} has a return type, constructors don’t have return types
}
