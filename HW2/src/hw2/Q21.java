package hw2;

public class Q21 {
    int Integer = 0;   // k1 valid but not preferable because, “Integer” is name of a wrapper class
    //integer int = 0; // k2 there is no type named integer and int is a name of a primitive type, invalid
    //Integer ++;      // k3 Integer is name of a wrapper class, this is invalid
    //int ++;          // k4 int is a name of a primitive type, invalid
}
